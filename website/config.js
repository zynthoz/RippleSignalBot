// For deployment: Set to your API server URL (ngrok tunnel, Vercel, etc.)
// For local development: Set to 'http://localhost:3000' or use proxy
// Leave empty to use same-origin (if API and dashboard are served from same domain)
window.API_BASE_URL = 'https://matcher-matchbook-tragedy.ngrok-free.dev';
